<% if current_user && current_cart.cart_pots.size != 0 %>
  <h1>Your Cart</h1>

  <% current_cart.pots.each_with_index do |pot, i| %>
    <ul>
      <li>Pot: 
        <%= pot.name %>
        <%= pot.dimensions %>
        <%= pot.material %>
        <%= pot.price %>

        <p>Flowers:
        <% current_cart.cart_pots[i].flowers.each_with_index do |flower, j| %>
          <%= flower.name %>(<%= current_cart.cart_pots[i].cart_flowers[j].units %>)
        <% end %>
        </p>

        <p>Individual Price: <%= current_cart.cart_pots[i].total_price %></p>
      </li>
    </ul>
  <% end %>
  <h3>Total price: <%= current_user.total_orders_price_in_cart %> </h3>
<% end %>